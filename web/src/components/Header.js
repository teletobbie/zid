import Navigation from "../navigation/Navigation"

/**
 * Create and returns a JSX Header Element
 * @returns {JSX.Element} JSX Header Element 
 */
const Header = () => {
    return (
    <header>
      <Navigation />
    </header>
  )
}

export default Header
  
